# Skeleton — wire services in bootstrap (plan §2, §11).
# Intended services: api, frontend, chroma, optional langfuse (profile).

services:
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8001:8000"
    volumes:
      - chroma_data:/chroma/chroma

volumes:
  chroma_data:

# Frontend

Next.js 15 App Router frontend for AetherMind.

## Setup

- Copy `.env.local.example` to `.env.local`.
- Install deps: `npm install`
- Run dev server: `npm run dev`

## Tests

- Lint: `npm run lint`
- Build: `npm run build`
- E2E (mocked backend): `npm run test:e2e`
